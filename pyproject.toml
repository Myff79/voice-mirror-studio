[project]
name = "gradium-livekit-student"
version = "0.1.0"
requires-python = ">=3.10,<3.14"
dependencies = [
  "livekit-agents[deepgram,gradium]~=1.6",
  "python-dotenv>=1.0",
]

[tool.uv]
package = false
